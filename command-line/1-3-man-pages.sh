# According to the man page, what is the official description of of echo on your system:

man echo
# echo -- write arguments to the standard output
# The echo utility writes any specified operands, separated by single blank (` ') characters and followed by a newline (`\n') character, to the standard output.

# DEtermine the command needed to print out "hello" without the trailing newline:

echo -n
#do not print the trailing newline character

man man
#use man to view man pages for different unix commands
